# reference

Teaching examples — queries kept because they illustrate a KQL concept rather than solve a specific operational problem.

| Query | Description |
| --- | --- |
| [`sort-function-result-comparison.kql`](./sort-function-result-comparison.kql) | Side-by-side of `sort by` vs `top` on a cost-per-EventID query, showing that both produce identical results in this case — a small worked example for anyone learning KQL sort semantics. |
